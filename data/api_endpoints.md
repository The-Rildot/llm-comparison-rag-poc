# API Endpoints

## GET /api/assets
Returns a list of assets visible to the authenticated user.

## POST /api/assets/{id}/delete
Deletes the asset.

### Notes
- This endpoint requires Admin or user privileges
- There is no endpoint for renaming assets